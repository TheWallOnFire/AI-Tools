"""
Problem: Strong Password Checker

Description:
A strong password has: at least 6 and at most 20 characters; at least one lowercase letter, one uppercase letter, and one digit; does not contain three repeating characters in a row. Return the minimum number of steps required to make a password strong.

Constraints:
1 <= password.length <= 50, password consists of English letters, digits, and punctuation marks.

Example:
Input: password = "a" -> Output: 5
"""

class Solution:
    def strongPasswordChecker(self, password: str) -> int:
        pass
