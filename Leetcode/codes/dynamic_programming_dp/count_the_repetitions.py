"""
Problem: Count The Repetitions

Description:
We define str = [s, n] as the string s repeated n times. For example, ["abc", 3] ="abcabcabc". We say that string s1 can be obtained from string s2 if we can remove some characters from s2 such that it becomes s1. Given two strings s1 and s2 and two integers n1 and n2, return the maximum integer m such that [s2, m] can be obtained from [s1, n1].

Constraints:
1 <= s1.length, s2.length <= 100, 1 <= n1, n2 <= 10^6, s1 and s2 consist of lowercase English letters.

Example:
Input: s1 = "acb", n1 = 4, s2 = "ab", n2 = 2 -> Output: 2
"""

class Solution:
    def getMaxRepetitions(self, s1: str, n1: int, s2: str, n2: int) -> int:
        pass
