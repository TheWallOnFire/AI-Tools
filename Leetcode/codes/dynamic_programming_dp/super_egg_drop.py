"""
Problem: Super Egg Drop

Description:
You are given k identical eggs and you have access to a building with n floors labeled from 1 to n. You know that there exists a floor f such that 0 <= f <= n. Each egg, if dropped from a floor higher than f, will break. If it is dropped from a floor f or below, it will not break. Return the minimum number of moves that you need to determine with certainty what the value of f is.

Constraints:
1 <= k <= 100, 1 <= n <= 10^4.

Example:
Input: k = 1, n = 2 -> Output: 2
"""

class Solution:
    def superEggDrop(self, k: int, n: int) -> int:
        pass
