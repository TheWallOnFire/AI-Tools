"""
Problem: Compare Version Numbers

Description:
Given two version numbers, version1 and version2, compare them. Return 1 if version1 > version2, -1 if version1 < version2, and 0 otherwise.

Constraints:
1 <= version1.length, version2.length <= 500, versions consist of digits and '.'

Example:
Input: version1 = "1.01", version2 = "1.001" -> Output: 0
"""

class Solution:
    def compareVersion(self, version1: str, version2: str) -> int:
        pass
