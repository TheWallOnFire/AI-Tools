"""
Problem: Decode Ways II

Description:
A message containing letters from A-Z is being encoded for communications using the following mapping: 'A' -> "1", 'B' -> "2", ..., 'Z' -> "26". However, the message string can also contain the character '*', which can be treated as one of the digits from 1 to 9. Return the total number of ways to decode it.

Constraints:
1 <= s.length <= 10^5, s[i] is a digit or '*'.

Example:
Input: s = "*" -> Output: 9
"""

class Solution:
    def numDecodings(self, s: str) -> int:
        pass
