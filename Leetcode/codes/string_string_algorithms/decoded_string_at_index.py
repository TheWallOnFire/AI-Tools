"""
Problem: Decoded String at Index

Description:
You are given an encoded string s and an integer k. To decode the string to a tape, the encoded string is read one character at a time. If the character is a letter, that letter is written onto the tape. If the character is a digit d, the entire current tape is repeatedly written d-1 more times in total. Return the k-th letter (1-indexed) in the decoded string.

Constraints:
2 <= s.length <= 100, s consists of lowercase English letters and digits 2 through 9, s starts with a letter, 1 <= k <= 10^9, it is guaranteed that the k-th letter exists.

Example:
Input: s = "leet2code3", k = 10 -> Output: "o"
"""

class Solution:
    def decodeAtIndex(self, s: str, k: int) -> str:
        pass
