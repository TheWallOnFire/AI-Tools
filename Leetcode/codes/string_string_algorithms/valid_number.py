"""
Problem: Valid Number

Description:
A valid number can be split into a significand and optionally an exponent. The significand is either a decimal number or an integer. The exponent is an 'e' or 'E' followed by an integer.

Constraints:
1 <= s.length <= 20, s consists of English letters (only 'e' and 'E'), digits, '.', '+', and '-'.

Example:
Input: s = "0" -> Output: true
"""

class Solution:
    def isNumber(self, s: str) -> bool:
        pass
