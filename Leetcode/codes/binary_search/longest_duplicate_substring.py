"""
Problem: Longest Duplicate Substring

Description:
Given a string s, consider all duplicated substrings: (contiguous) substrings of s that occur 2 or more times. The occurrences may overlap. Return any duplicated substring that has the longest possible length. If s has no duplicated substring, return "".

Constraints:
2 <= s.length <= 3 * 10^4, s consists of lowercase English letters.

Example:
Input: s = "banana" -> Output: "ana"
"""

class Solution:
    def longestDupSubstring(self, s: str) -> str:
        pass
