"""
Problem: Longest Common Subpath

Description:
There is a country of n cities numbered from 0 to n - 1. You are given a 2D integer array paths where paths[i] is a list of cities that the i-th person visited in order. Return the length of the longest common subpath that is shared by every person's path, or 0 if there is no common subpath at all.

Constraints:
1 <= n <= 10^5, m == paths.length, 2 <= m <= 10^5, sum(paths[i].length) <= 10^5, 0 <= paths[i][j] < n.

Example:
Input: n = 5, paths = [[0,1,2,3,4],[2,3,4],[4,0,1,2,3]] -> Output: 2
"""

class Solution:
    def longestCommonSubpath(self, n: int, paths: List[List[int]]) -> int:
        pass
