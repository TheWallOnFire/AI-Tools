"""
Problem: One Edit Distance

Description:
Given two strings s and t, return true if they are both one edit distance apart, otherwise return false. A string s is said to be one edit distance apart from a string t if you can: Insert exactly one character into s to get t; Delete exactly one character from s to get t; Replace exactly one character of s with a different character to get t.

Constraints:
0 <= s.length, t.length <= 10^4, s and t consist of lowercase English letters, uppercase English letters, and digits.

Example:
Input: s = "ab", t = "acb" -> Output: true
"""

class Solution:
    def isOneEditDistance(self, s: str, t: str) -> bool:
        pass
