"""
Problem: Minimize the Difference Between Target and Chosen Elements

Description:
You are given an m x n integer matrix mat and an integer target. Choose one integer from each row in the matrix such that the absolute difference between the sum of the chosen elements and target is minimized. Return the minimum absolute difference.

Constraints:
m == mat.length, n == mat[i].length, 1 <= m, n <= 70, 1 <= mat[i][j] <= 70, 1 <= target <= 800.

Example:
Input: mat = [[1,2,3],[4,5,6],[7,8,9]], target = 13 -> Output: 0
"""

class Solution:
    def minimizeTheDifference(self, mat: List[List[int]], target: int) -> int:
        pass
