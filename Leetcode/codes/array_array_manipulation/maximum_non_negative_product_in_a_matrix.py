"""
Problem: Maximum Non Negative Product in a Matrix

Description:
You are given a m x n matrix grid. Initially, you are at the cell (0, 0) and you want to reach the cell (m - 1, n - 1). In each step, you can only move right or down in the grid. For a possible path from (0, 0) to (m - 1, n - 1), we define the product of the path as the product of all numbers in the cells visited of the path.

Constraints:
m == grid.length, n == grid[i].length, 1 <= m, n <= 15, -4 <= grid[i][j] <= 4.

Example:
Input: grid = [[-1,-2,-3],[-2,-3,-3],[-3,-3,-2]] -> Output: -1
"""

class Solution:
    def maxProductPath(self, grid: List[List[int]]) -> int:
        pass
