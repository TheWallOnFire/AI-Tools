"""
Problem: Dungeon Game

Description:
The demons had captured the princess and imprisoned her in the bottom-right corner of a dungeon. The dungeon consists of m x n rooms laid out in a 2D grid. Our valiant knight was initially positioned in the top-left room and must fight his way through the dungeon to rescue the princess. Return the knight's minimum initial health so that he can rescue the princess.

Constraints:
m == dungeon.length, n == dungeon[i].length, 1 <= m, n <= 200, -1000 <= dungeon[i][j] <= 1000.

Example:
Input: dungeon = [[-2,-3,3],[-5,-10,1],[10,30,-5]] -> Output: 7
"""

class Solution:
    def calculateMinimumHP(self, dungeon: List[List[int]]) -> int:
        pass
