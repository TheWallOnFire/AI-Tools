"""
Problem: Maximum Number of Visible Points

Description:
You are given an array points, an integer angle, and your location, where location = [posx, posy] and points[i] = [xi, yi] both denote integral coordinates on the X-Y plane. Return the maximum number of points you can see given your location and the viewing angle.

Constraints:
1 <= points.length <= 10^5, points[i].length == 2, location.length == 2, 0 <= angle < 360, 0 <= posx, posy, xi, yi <= 100.

Example:
Input: points = [[2,1],[2,2],[3,3]], angle = 90, location = [1,1] -> Output: 3
"""

class Solution:
    def visiblePoints(self, points: List[List[int]], angle: int, location: List[int]) -> int:
        pass
