"""
Problem: Make Sum Divisible by P

Description:
Given an array of positive integers nums and a positive integer p, remove the smallest subarray (possibly empty) such that the sum of the remaining elements is divisible by p. Return the length of the smallest subarray that you removed, or -1 if it's impossible.

Constraints:
1 <= nums.length <= 10^5, 1 <= nums[i] <= 10^9, 1 <= p <= 10^9.

Example:
Input: nums = [3,1,4,2], p = 6 -> Output: 1
"""

class Solution:
    def minSubarray(self, nums: List[int], p: int) -> int:
        pass
