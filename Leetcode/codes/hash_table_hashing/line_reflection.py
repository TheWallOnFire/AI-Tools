"""
Problem: Line Reflection

Description:
Given n points on a 2D plane, find if there is such a line parallel to y-axis that reflect the given points symmetrically, in other words, answer whether or not if there exists a line that reflect the points symmetrically.

Constraints:
n == points.length, 1 <= n <= 10^4, -10^8 <= points[i][j] <= 10^8.

Example:
Input: points = [[1,1],[-1,1]] -> Output: true
"""

class Solution:
    def isReflected(self, points: List[List[int]]) -> bool:
        pass
